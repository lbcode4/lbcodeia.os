{
  "name": "lbcodeia-sistema",
  "version": "1.0.0",
  "private": true,
  "description": "Sistema operacional do negócio (LBCode.IA). Deps usadas pelas skills de conteúdo (geração de imagem IA + render de carrossel/site via Playwright).",
  "type": "commonjs",
  "scripts": {
    "postinstall": "playwright install chromium"
  },
  "dependencies": {
    "@google/genai": "^1.5.0",
    "openai": "^4.67.0",
    "playwright": "^1.48.0"
  }
}
